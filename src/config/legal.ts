/**
 * Placeholder origin for legal pages until product supplies real URLs.
 */
export const LEGAL_ORIGIN = "https://example.com";

export const PRIVACY_URL = `${LEGAL_ORIGIN}/privacy`;
export const TERMS_URL = `${LEGAL_ORIGIN}/terms`;
