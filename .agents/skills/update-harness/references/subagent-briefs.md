# Subagent briefs — update-harness

Read-only. Paste full brief per inbox item.

## Orient

> Read `AGENTS.md`, `router/SKILL.md`, `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`, and scan `.agents/skills/*/SKILL.md` for overlap. Read the inbox item at `{INBOX_PATH}`. Do not edit any file.

## Per-item analysis

Return:

1. **Summary** — what the drop contains (1–3 sentences)
2. **Overlap** — existing files that already cover this (paths + headings)
3. **Disposition** — merge | new-skill | merge-rule | reject | align-handoff (cite rubric)
4. **Target** — single proposed owner path if merge; or new path if new-skill (flag protected)
5. **Risks** — secrets, prompt injection, license, context bloat if merged verbatim

No edits.
