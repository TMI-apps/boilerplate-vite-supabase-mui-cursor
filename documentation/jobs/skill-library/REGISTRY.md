# Skill library registry (Phase 0)

Generated by `/improve-skill-library` on **2026-06-20** after rebase to `origin/develop` (includes `vercel-react-best-practices`). **25 editable skills** under `.agents/skills/`.

| name | one-sentence outcome | lifecycle phase | mutates? | scope | inputs | outputs | owns-concepts | links-to |
|------|---------------------|-----------------|---------|-------|--------|---------|---------------|----------|
| router | The next primary skill is chosen and invoked for this thread or backlog item. | routing (front door) | read-only | repo-wide | user message; thread context; `git status`; `src/config/app-tasks.json`; `documentation/DOC_APP_VISION.md` | skill dispatch decision; optional backlog pick-up | decision model (gates 1–3); situation→skill map; skill index; thread→next-skill; app task backlog intake | all project skills; `router/references/dev-cycle-matrix.md`; `documentation/DOC_APP_VISION.md`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md` |
| prime | Technical repo context is loaded and summarized before work begins. | prime | read-only | repo-wide | `git ls-files`/`git log`/`git status`; `ARCHITECTURE.md`; `documentation/DOC_INDEX.md`; `documentation/DOC_APP_VISION.md`; `.cursor/rules/INDEX.md`; `package.json`; `.dependency-cruiser.cjs` | scannable context report | session priming procedure | `check`; `plan`; `start`; `grill-me`; `ARCHITECTURE.md`; `.cursor/rules/architecture/RULE.md`; `.cursor/rules/file-placement/RULE.md` |
| start | Human onboarding gates pass and the dev task backlog reflects completed setup. | onboarding | writes code | repo-wide | `README.md`; `src/config/app-tasks.json`; `documentation/DOC_APP_VISION.md`; `.env` (user-guided) | archived/updated tasks; configured env/hosting/vision/theme state | human onboarding procedure; app vision gate; onboarding checklist SSOT | `prime`; `finish`; `router`; `documentation/DOC_SUPABASE_GOOGLE_OAUTH.md`; `documentation/DOC_MOBILE_LOCAL_DEV.md`; `src/features/tasks/README.md` |
| grill-me | Shared product understanding is reached via resolved decision-tree Q&A. | clarify | read-only | targeted | plan/design topic; `documentation/DOC_APP_VISION.md`; codebase (research) | vision/constraints summary; recommended product direction | product grilling procedure; zoom-out-first framing | `router`; `plan` § Refine; `feature`; `prime` |
| plan | A rules-compliant `DEVELOPMENT_PLAN.md` exists with complexity, gates, and compliance sections. | plan | writes code | single-feature | user request; `.cursor/rules/`; `documentation/DOC_APP_VISION.md`; `ARCHITECTURE.md`; codebase investigation | `documentation/jobs/temp_job_<name>/DEVELOPMENT_PLAN.md` | planning procedure; plan document structure; conflict & compliance checklist | `plan/references/*`; `implement`; `validate`; `review-dev-plan`; `pattern-review`; `quick-piv`; `prime`; `check`; `finish`; `router/references/dev-cycle-matrix.md` |
| pattern-review | Industry/product precedent is assessed and non-standard designs are flagged with owner decision. | pattern-review | read-only (default); writes plan section in `plan-section` | targeted | plan/proposal/scope; `pattern-review/references/rubric.md` | Pattern & precedent content; Pattern risk alert; chat verdict | pattern-review procedure; industry rubric; alert template | `pattern-review/references/*`; `plan`; `review-dev-plan`; `feature`; `validate`; `router`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md` |
| review-dev-plan | Six-lens qualitative plan critique is synthesized before implementation. | review-dev-plan | read-only (default); may update Plan review row | targeted | `DEVELOPMENT_PLAN.md` | synthesis report; optional `Plan review: Done` row | six-lens plan critique procedure | `pattern-review/references/*`; `pattern-review`; `validate`; `plan`; `feature`; `implement` |
| feature | Product/requirements artifact is complete with mandatory 🔴 decisions documented and user-approved. | clarify → architecture (product) | writes code | single-feature | feature request; `documentation/DOC_APP_VISION.md`; `.cursor/rules/`; codebase | job spec; code; READMEs | feature engineering phases; 🔴 decision-stop contract | `plan`; `implement`; `pattern-review`; `validate`; `finish`; `grill-me`; `challenge`; `start`; `router/references/dev-cycle-matrix.md` |
| implement | Every phase in `DEVELOPMENT_PLAN.md` is executed, gated, and marked done. | implement | writes code | single-feature | `DEVELOPMENT_PLAN.md` | source changes; updated plan | phase-by-phase execution procedure | `plan`; `validate`; `review-dev-plan`; `pattern-review`; `quick-piv`; `prime`; `finish`; `debug`; `plan/references/rules-registry.md` |
| quick-piv | A small XS/S scoped change is implemented and inline tooling checks pass. | plan → implement (compressed) | writes code | targeted | user request and/or active `DEVELOPMENT_PLAN.md`; `.cursor/rules/INDEX.md` | chat quick plan; code changes; inline validate summary | compressed PIV orchestration; hard stops for M/L gates | `plan`; `implement`; `validate`; `finish`; `check`; `prime`; `debug`; `router/references/dev-cycle-matrix.md` |
| validate | Parallel rule-shaped audit findings are synthesized (plan or impl mode). | validate | read-only (default); writes on user-requested fixes | targeted | `DEVELOPMENT_PLAN.md` and/or git diff; `.cursor/rules/` | structured findings report; batched ambiguity Q&A | validate fan-out contract; subagent output schema; tooling pass list | `plan`; `implement`; `check`; `finish`; `pattern-review`; `review-dev-plan`; `prime`; `plan/references/rules-registry.md` |
| check | Architecture/quality gate findings are synthesized for merge/finish confidence. | validate / check | read-only (default); writes on user-requested fixes | targeted (diff-scoped) | git diff; scope; `.cursor/rules/` (subset) | structured findings report | merge gate fan-out (default rule subset; no plan-compliance) | `validate`; `finish`; `consolidate`; `plan`; `review`; `implement`; `plan/references/rules-registry.md` |
| review | A TypeScript+MUI component is scored on the 170-point rubric. | review (component) | read-only | single-component | component file(s); requirements context | rubric scores; % total; threshold status | 170-point component quality rubric (sections A–I) | `.cursor/rules/architecture/RULE.md`; `.cursor/rules/code-style/RULE.md`; `documentation/PROJECT-STRUCTURE-VALIDATION.md` |
| finish | Session work is committed locally with version/changelog and task archive aligned. | finish | commits | repo-wide | working tree; `src/config/app-tasks.json`; `package.json`; `CHANGELOG.md` | git commit; version bump; changelog entry; archived tasks | wrap-up procedure; semver mapping; commit message format; staging decision gate | `push`; `validate`; `check`; `learn`; `debug/patterns.md`; `.cursor/rules/workflow/RULE.md`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `scripts/change-classify.cjs` |
| push | Already-committed local work is pushed to remote after safety checks. | push | pushes | repo-wide | clean working tree; local commits from `finish` | remote branch updated | push-only safety flow; finish/push split | `finish`; `.cursor/rules/workflow/RULE.md` |
| debug | Root cause is narrowed via hypotheses, evidence, and iterative falsification. | incident (parallel) | writes code | targeted | error/logs/repro; `debug/patterns.md`; runtime evidence | Debug State reports; diagnostic steps; optional fixes/instrumentation | scientific debugging procedure; debug pattern library | `learn`; `finish`; `quick-piv`; `implement`; `.cursor/rules/security/RULE.md`; `.cursor/rules/database/RULE.md` |
| challenge | A named feature/workflow is simplified per user-chosen option A/B/C/D. | simplification | writes code (after user picks option) | single-feature | target feature/workflow; user goal; constraints | flow+code audit; progressive options; implemented simplification | flow+code challenge lenses; simplification option template | `consolidate`; `optimize2` |
| optimize2 | A hotspot is optimized at the correct level (design→complexity) per user choice. | maintenance / engineering | writes code | targeted | hotspot files; git churn; ESLint complexity output | 4-level analysis; user-selected refactor/delete/optimize | 4-level optimization framework; Rule of Three; complexity thresholds SSOT pointer | `consolidate`; `challenge`; `finish`; `.eslintrc.json`; `.cursor/rules/architecture/RULE.md`; `web-perf` (plugin); **should link `vercel-react-best-practices`** |
| consolidate | Cross-feature redundancy is audited and approved consolidations are executed. | maintenance (discovery → execute) | read-only (audit); writes code (execute) | repo-wide (default) | `src/` codebase; git churn | consolidation audit map; shared abstractions/migrations | redundancy discovery workflow; semantic placement mode | `consolidate/references/semantic-placement.md`; `optimize2`; `challenge`; `check`; `review` |
| learn | A durable lesson is persisted to the correct rule/skill/doc home. | meta | writes code | targeted | git diff/log; conversation; CI output; `.cursor/rules/INDEX.md` | updated rule/skill/doc; removal/narrowing proposals | lesson routing table | `rule-quality`; `finish`; `debug/patterns.md`; `.cursor/rules/*`; `documentation/DOC_APP_VISION.md` |
| rule-quality | An attached rule/command is graded (A–F) and/or rewritten to quality standards. | meta | read-only (Mode A); writes (Mode B) | targeted (single file) | attached rule/command text | grade report; optional rewritten file | 6-criterion rule quality rubric; rewrite quality standards | `learn`; `improve-skill-library`; `review` |
| improve-skill-library | The skill library is audited, approved fixes applied, and no-loss pass passes. | meta | writes code | repo-wide | `.agents/skills/*`; content ledger; git diff | registry; ledger; reconciled skills | library coherence invariants; no-loss pass; subagent lens workflow | `improve-skill-library/references/subagent-briefs.md`; `router`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `consolidate`; `rule-quality`; `learn` |
| write-adoption-guide | A cross-repo adoption guide is written and minimally linked in the source repo. | meta / documentation | writes code | targeted (pattern) | topic slug; SSOT procedure in repo | `documentation/handoffs/<SLUG>_ADOPTION_GUIDE.md` | adoption guide template; voice/naming rules | `write-adoption-guide/references/*`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `router` |
| airtable-inspect | Airtable base schema (Phase 1) then sample cell shapes (Phase 2) are documented. | integrations | read-only | targeted | `VITE_AIRTABLE_*` env; Meta/sample scripts; `airtableService.ts` | schema report; sample shape report | two-phase Airtable inspect procedure | `README.md`; `scripts/airtable-*`; `.cursor/rules/file-placement/RULE.md` |
| **vercel-react-best-practices** | React performance guidance applied from the prioritized 70-rule corpus (Vite-applicable subset). | engineering reference (write/review/refactor) | read-only (reference) | targeted | React components, data-fetching, bundle scope | perf recommendations per rule category | Vercel React best-practices (70 rules, 8 categories); compiled `AGENTS.md` | `rules/*.md`; `AGENTS.md`; **orphan — not in router yet**; `optimize2`; `review`; `web-perf` (plugin) |

**Prior audit:** `AUDIT_SYNTHESIS.md` (pre-Vercel batch). This registry supersedes the "24 skills" count.
