# Skill registry — improve-skill-library Phase 0

Captured 2026-07-03 before any edits. One row per editable skill under `.agents/skills/`.

| name | one-sentence outcome ("done") | lifecycle phase | mutates? | scope | inputs (artifacts consumed) | outputs (artifacts produced) | owns-concepts (SSOT it claims) | links-to (skills/docs referenced) |
|---|---|---|---|---|---|---|---|---|
| airtable-inspect | Phase 1 schema and optional Phase 2 sample cell shapes are documented for the target base/table without leaking secrets. | integrations | read-only | targeted | `VITE_AIRTABLE_*` env; Meta/Data API; `src/shared/services/airtableService.ts`; `scripts/airtable-meta-dump.js`, `scripts/airtable-sample-records.js` | Chat schema/sample report; optional local `meta.json` dump (outside git) | Two-phase Airtable inspect procedure (schema → sample); security/redaction defaults | `start`; `.cursor/rules/file-placement/RULE.md`, `architecture/RULE.md`; README Airtable scripts |
| bundle-ship | Full working-tree snapshot is bundled into one `finish` commit and pushed in one invocation after user confirms. | finish → push | commits + pushes | repo-wide | Entire dirty working tree; `finish`, `push` procedures; `src/config/app-tasks.json` | Single bundled commit; remote push | Multi-thread same-checkout bundle orchestration (delegates commit/push SSOT to `finish`/`push`) | `finish`, `push`, `validate`; `workflow/RULE.md` |
| caveman | Assistant replies in ultra-compressed mode until user says stop/normal mode. | meta (overlay) | read-only | repo-wide | Any user/agent messages | Terse chat responses | Ultra-compressed communication mode rules | — |
| challenge | Named feature/workflow is mapped, simplification options presented, and chosen option handed to `quick-piv`/`plan` for delivery. | engineering (product shape) | read-only (report; delivery via handoff) | single-feature | Target feature/workflow; user goal/constraints; implementation files | Challenge report + option pick (handoff input for `quick-piv`/`plan`) | Flow + code challenge lenses; progressive options A–D; simplify-before-optimize order | `quick-piv`, `plan`, `implement`, `consolidate`, `optimize2`, `finish` |
| consolidate | Redundancy audit map is delivered and user-approved consolidations are migrated with verification. | engineering (repo maintenance) | writes code | repo-wide (default); targeted scoping optional | `src/` codebase; `architecture/RULE.md`; `projectStructure.config.cjs` | Consolidation audit report; optional shared abstractions/refactors | Redundancy discovery → classify → prioritize workflow; Rule of Three (links `optimize2`); semantic placement mode (`references/semantic-placement.md`) | `optimize2`, `validate`, `challenge`, `implement`, `quick-piv`, `review` |
| debug | Each iteration narrows root cause with evidence until the user declares the incident resolved. | engineering (incident) | writes code | targeted | Error messages/logs; user repro steps; `patterns.md`; optional Debug Mode session | Debug state reports; instrumentation/fixes (revert rejected hypotheses) | Scientific-method debugging procedure; preflight slots; event-chain reconstruction | `hypothesis`, `learn`, `finish` (`patterns.md`); `quick-piv`, `implement`; `security/RULE.md`, `database/RULE.md` |
| feature | Product/requirements artifact is complete with all 🔴 decisions documented and user-approved for handoff to engineering plan. | clarify → plan (product) | read-only (app code); writes requirements artifact — **never** `DEVELOPMENT_PLAN.md` | single-feature | `documentation/DOC_APP_VISION.md`; user decisions; rules via `plan/references/rules-registry.md` | Requirements/spec artifact in chat/doc (declared `plan` input); journey/state maps; handoff to `plan` | New-feature engineering phases with mandatory 🔴 decision stops; product SSOT alignment (not `DEVELOPMENT_PLAN.md`) | `plan`, `pattern-review`, `review-dev-plan`, `validate`, `implement`, `finish`, `grill-me`, `challenge`, `start`; `router/references/dev-cycle-matrix.md` |
| finish | Session work is commit-ready locally with version/changelog when required and tasks archived when applicable. | finish | commits | targeted (session scope); repo-wide in bundle mode | Staged/unstaged diff; `scripts/change-classify.cjs`; `src/config/app-tasks.json`; `package.json`, `CHANGELOG.md` | Local commit; archived tasks; version/changelog updates; optional `debug/patterns.md` entry | Commit message standards; semver mapping; staging decision gate; pre-commit light-path refs; concurrent-agent smoke handling | `push`, `validate`, `learn`, `debug`; `workflow/RULE.md`, `DOC_AGENT_WORKFLOW_LAYERS.md`, `DOC_CHANGESETS.md` |
| grill-me | Shared understanding of scope edges, non-goals, and neighbor boundaries is reached in chat with no artifact authored. | clarify | read-only | single-feature | User answers; `documentation/DOC_APP_VISION.md`; optional codebase exploration | Chat summary (vision, in/out scope, non-goals, boundary map) | Scope-edge alignment in conversation (not recording); ride-vs-new boundary questioning | `router`, `plan` § Refine, `feature`, `quick-piv`, `prime`; `DOC_APP_VISION.md` |
| hypothesis | Pre-registered hypothesis block is written, fix tested, and candidate cause space shrinks each iteration. | engineering (incident **overlay of `debug`** — explicit invoke only) | writes code | targeted | Active `debug` incident; prior ruled-out causes; user test results | `HYPOTHESIS` blocks; `RULED OUT` / `STILL OPEN` tallies; targeted fixes | Pre-registered hypothesis experiment procedure (IF PROVED / IF DISPROVED) | `debug`, `router`, `learn`, `finish`; `debug/patterns.md` via `finish`/`learn` |
| implement | All `DEVELOPMENT_PLAN.md` phases are executed, gates passed, and plan progress sections updated. | implement | writes code | single-feature | `documentation/jobs/temp_job_*/DEVELOPMENT_PLAN.md`; `.cursor/rules/*` | Application code changes; updated plan (phase status, Notes, Decisions) | Phase-by-phase plan execution procedure | `plan`, `validate`, `finish`, `review-dev-plan`, `pattern-review`, `prime`, `quick-piv`, `debug`; `plan/references/rules-registry.md` |
| improve-skill-library | Skill corpus is audited, approved fixes applied, and no-information-loss pass passes with zero MISSING units. | meta | writes code | repo-wide | `.agents/skills/*/SKILL.md` (+ `references/`); `router/SKILL.md`; `DOC_AGENT_WORKFLOW_LAYERS.md`; user/plugin skills (read-only) | Registry + content ledger under `documentation/jobs/skill-library/`; edited skills/router/layers doc | Skill-library coherence invariants; concern-space axes; parallel lens audit; no-loss gate | `router`, `consolidate`, `rule-quality`, `learn`, `validate`; `references/subagent-briefs.md`; `~/.cursor/skills-cursor/create-skill` |
| learn | Durable lesson is persisted to one primary home with reverse-audit removals presented first. | meta | writes code | repo-wide | Recent diff/errors/conversation; `.cursor/rules/INDEX.md`; `.agents/skills/` | Updated rule/skill/doc subsection; TL;DR report | Lesson routing table (where each lesson type lives); reverse-audit procedure | `rule-quality`, `finish`, `debug/patterns.md`; `.cursor/rules/*`, `DOC_APP_VISION.md` |
| optimize2 | Hotspot is analyzed across four levels, user picks options, and chosen changes are verified. | engineering | writes code | targeted | Named files/functions; `git` churn; `.eslintrc.json` complexity thresholds | Hotspot analysis report; refactored/deleted/optimized code | Four-level optimization framework (Design→Approach→Efficiency→Complexity); Rule of Three; indirection red flags | `consolidate`, `finish`, `react-perf-vite`; `web-perf` (plugin); `architecture/RULE.md`, `file-placement/RULE.md` |
| pattern-review | Industry precedent verdict is delivered and plan § Pattern & precedent is filled when required. | pattern-review (during plan) | read-only by default; writes plan section when `plan-section` | targeted | Plan/proposal; `references/rubric.md`, `references/alert-template.md` | Chat verdict; Pattern risk alert; `DEVELOPMENT_PLAN.md` § Pattern & precedent | Industry/product precedent procedure; rubric; alert template | `plan`, `review-dev-plan`, `validate`, `feature`, `router`; `DOC_AGENT_WORKFLOW_LAYERS.md`; `plan/references/implementation-plan-template.md` |
| plan | `DEVELOPMENT_PLAN.md` exists with phases, gates, Complexity, compliance, and review gates set. | plan | writes code (docs) | single-feature | User request; `documentation/DOC_APP_VISION.md`; codebase/rules research | `documentation/jobs/temp_job_<name>/DEVELOPMENT_PLAN.md` | Engineering execution plan authoring; § Refine gate-2 clarification; conflict & compliance checklist | `prime`, `implement`, `validate`, `quick-piv`, `review-dev-plan`, `pattern-review`, `finish`, `grill-me`, `optimize2`; `references/implementation-plan-template.md`, `complexity-rubric.md`, `rules-registry.md`; `router/references/dev-cycle-matrix.md` |
| prime | Concise project context report covers structure, governance, branch state, and actionable insights. | prime | read-only | repo-wide | `git` state; `ARCHITECTURE.md`; `.cursor/rules/INDEX.md`; `documentation/DOC_INDEX.md`, `DOC_APP_VISION.md`; `package.json`; `.dependency-cruiser.cjs` | Chat context report | Session technical context loading procedure | `start`, `plan`, `validate`, `grill-me`, `finish`; `documentation/jobs/` |
| push | Already-committed changes are pushed to remote after safety checks and user confirmation. | push | pushes | targeted | Clean tree; local commits ahead of remote; branch context | Remote branch update | Push-only safety flow (no commit operations) | `finish`; `workflow/RULE.md` |
| quick-piv | XS/S scoped change is implemented with chat quick-plan and inline tooling pass in one session. | plan → implement → validate (compressed) | writes code | targeted | User request; optional `DEVELOPMENT_PLAN.md` extension; rules index | Code changes; chat quick plan; optional plan doc updates | Compressed Plan→Implement→Validate orchestration for XS/S | `plan`, `implement`, `validate`, `finish`, `prime`, `debug`; `router/references/dev-cycle-matrix.md` |
| react-perf-vite | Relevant stack-native perf rules are read and applied for the stated symptom. | engineering (reference) | read-only | targeted | Perf symptom (waterfall, bundle, re-render, etc.) | Guidance from `rules/*.md`; handoff to `optimize2` when structural | Stack-native React perf rule index (`async-`, `bundle-`, `rerender-`, etc.) | `optimize2`, `web-perf` (plugin), `review`; `DOC_REACT_PERF.md`, `DOC_TANSTACK_QUERY.md`, `architecture/RULE.md` |
| review | Target component receives a scored 170-point rubric with percentage and threshold status. | review | read-only | single-component | Named TS/MUI component file(s) | Scored rubric report (/170) | Component quality rubric (sections A–I) | `validate`, `quick-piv`, `implement`, `finish`; `architecture/RULE.md`, `code-style/RULE.md` |
| review-dev-plan | Six-lens critique is synthesized and Plan review status is updated when user accepts. | review-dev-plan | read-only by default; optional plan metadata edits | targeted | `documentation/jobs/**/DEVELOPMENT_PLAN.md`; architecture/rules context | Chat synthesis; `Plan review: Done` row; optional Decisions made rows | Multi-lens qualitative plan critique (rebel, scale/mod, industry precedent + 3 agent-picked lenses) | `pattern-review` (`rubric.md`, `alert-template.md`), `validate`, `plan`, `feature`, `implement` |
| router | One primary skill is chosen for the thread state and its workflow is invoked in the same turn. | meta (routing across lifecycle) | read-only | repo-wide | User message; thread state; `git status`; `src/config/app-tasks.json`; active `DEVELOPMENT_PLAN.md` | Skill invocation (no file artifact) | Three-gate decision model; thread continuation rules; app task backlog intake; skill overlap tiebreaks | All project skills; `documentation/DOC_APP_VISION.md`; `router/references/dev-cycle-matrix.md`; user/plugin skills (reference-only) |
| rule-quality | Attached rule/command quality is assessed — one deliverable per invocation via mandatory mode gate (grade **or** rewrite). | meta / rules | read-only (Mode A); writes with protected-file approval (Mode B) | targeted | Attached `.cursor/rules/**/RULE.md`, `SKILL.md`, or command text | Grade report (A–F) **or** improved file (never both unsolicited) | Rule/command quality rubric; rewrite quality standards | `learn`, `improve-skill-library`, `review` |
| start | Human onboarding gates pass per template/fork rules and user confirms ready to proceed. | prime (human onboarding) | writes code (config/docs/tasks) | repo-wide | `README.md`; `src/config/app-tasks.json`; user web-console actions | Configured env/hosting; `DOC_APP_VISION.md` ACTIVE (fork); archived/updated tasks; verification checklist green | Human first-time setup procedure; app vision gate; dev task backlog onboarding SSOT | `prime`, `plan`, `feature`; `DOC_APP_VISION.md`, `DOC_SUPABASE_GOOGLE_OAUTH.md`, `DOC_CLOUDFLARE_WORKERS.md`, `DOC_MOBILE_LOCAL_DEV.md`; `workflow/RULE.md` |
| validate | Mode-selected parallel rule audit completes and findings are synthesized before optional fixes. | validate / check | read-only by default; writes code if user requests fixes | targeted (diff/plan); repo-wide in gate mode | Plan path and/or `git diff`; `.cursor/rules/*`; tooling commands | Validation report with mode header and severity-grouped findings | Mode auto-selection (`plan-review` / `impl-full` / `gate`); subagent JSON schema; tooling pass lists | `pattern-review`, `review-dev-plan`, `review`, `finish`, `consolidate`; `plan/references/rules-registry.md`; `.cursor/rules/INDEX.md` |
| write-adoption-guide | Cross-repo `{SLUG}_ADOPTION_GUIDE.md` is written, voice-checked, and workflow layers doc is linked. | integrations / meta docs | writes code (docs) | targeted (one capability) | Implemented pattern; SSOT skill/rule/doc in source repo | `documentation/handoffs/{SLUG}_ADOPTION_GUIDE.md`; minimal layers-doc link | Adoption guide template; voice/naming rules; output folder configuration | `references/adoption-guide-template.md`, `voice-and-naming.md`, `layer-mapping.md`; `DOC_AGENT_WORKFLOW_LAYERS.md`; `router/SKILL.md` (optional index link) |
